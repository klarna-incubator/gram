{
  "data": {
    "_providers": {
      "postgres": {
        "host": "POSTGRES_HOST",
        "user": "POSTGRES_USER",
        "password": "POSTGRES_PASSWORD",
        "database": "POSTGRES_DB",
        "port": "POSTGRES_PORT"
      }
    }
  },

  "jwt": {
    "secret": {
      "auth": "AUTH_SECRET",
      "csrf": "CSRF_SECRET"
    }
  },

  "notifications": {
    "providers": {
      "email": {
        "user": "EMAIL_USER",
        "password": "EMAIL_PASSWORD",
        "host": "EMAIL_HOST",
        "port": "EMAIL_PORT"
      }
    }
  }
}
