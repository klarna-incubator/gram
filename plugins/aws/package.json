{
  "name": "@gram/aws",
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "version": "4.0.0",
  "description": "AWS icons for Gram",
  "scripts": {
    "build": "npm run clean && npm run compile",
    "clean": "rimraf ./dist",
    "compile": "tsc -p tsconfig.build.json"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@gram/core": "^4.0.0",
    "path": "^0.12.7"
  },
  "private": true,
  "devDependencies": {
    "@types/node": "^20.4.2"
  }
}
