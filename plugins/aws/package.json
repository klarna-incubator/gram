{
  "name": "@gram/aws",
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "version": "3.0.3",
  "description": "AWS icons for Gram",
  "scripts": {
    "build": "npm run clean && npm run compile",
    "clean": "rimraf ./dist",
    "compile": "tsc -p tsconfig.build.json"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@gram/core": "*",
    "path": "^0.12.7"
  },
  "private": true
}
